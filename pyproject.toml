[project]
name = "icml26-caffnet-hard-constraint-affine"
version = "0.1.0"
description = "Scoped clean-room reproduction of CAffNet: Hard Constraint-Affine Neural Networks."
requires-python = ">=3.12,<3.13"
dependencies = ["numpy==2.5.1", "scipy==1.18.0", "torch==2.13.0", "pytest==9.1.1"]
[tool.pytest.ini_options]
pythonpath = ["repro/src"]
